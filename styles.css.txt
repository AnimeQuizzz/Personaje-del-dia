body {
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  background: linear-gradient(to bottom right, #f7e1ff, #d2ecff);
  color: #333;
  text-align: center;
}

header {
  background-color: #fff0f5;
  padding: 1.5em 0;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

main {
  padding: 2em;
  display: flex;
  justify-content: center;
}

.card {
  background-color: white;
  border-radius: 20px;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
  padding: 2em;
  max-width: 400px;
  width: 100%;
}

.card img {
  width: 100%;
  border-radius: 15px;
  margin-bottom: 1em;
}

footer {
  margin-top: 3em;
  padding: 1em;
  font-size: 0.9em;
  background-color: #fff0f5;
}

input, button {
  padding: 0.6em;
  margin: 0.5em;
  font-size: 1em;
  border-radius: 10px;
  border: 1px solid #ccc;
}

button {
  background-color: #ffb6c1;
  border: none;
  cursor: pointer;
}

button:hover {
  background-color: #ffa3b1;
}
